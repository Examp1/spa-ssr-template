<div class="section-video-and-text">
    @if (!empty($content['title']))
        <div class="title">{{ $content['title'] }}</div>
    @endif
    @if (!empty($content['file']))
        <div>{{$content['file']}}</div>
    @endif
</div>

